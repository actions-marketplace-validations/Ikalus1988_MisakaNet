name: Intake Salvage Digest

on:
  schedule:
    - cron: '0 8 * * *'  # Daily at 08:00 UTC
  workflow_dispatch:

jobs:
  salvage-digest:
    runs-on: ubuntu-latest
    permissions:
      issues: write
    # Every other workflow that shells out to `gh` exports GH_TOKEN; this one did
    # not, so `gh` ran unauthenticated and exited 4. The step runs under `bash -e`
    # and the first call had `2>/dev/null`, so the failure was silent: the digest
    # failed every day from at least 2026-09-09 without a readable reason, and the
    # salvage queue it exists to surface was never surfaced.
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7
        with:
          python-version: "3.10"

      - name: Generate salvage digest
        run: |
          # Get all auto-rejected issues
          ISSUES=$(gh issue list --repo ${{ github.repository }} \
            --label "auto-rejected" \
            --state open \
            --json number,title,labels,createdAt \
            --limit 50)

          if [ "$ISSUES" = "[]" ] || [ -z "$ISSUES" ]; then
            echo "No rejected issues found."
            exit 0
          fi

          # Generate markdown digest
          DIGEST="# 📋 Intake Salvage Digest\n\n"
          # Quoted: `%H:%M UTC` contains a space, so unquoted `$(date -u +%Y-%m-%d %H:%M UTC)`
          # passes `%H:%M` as an extra operand, date exits 1, and `bash -e` kills
          # the step. This was the second failure under the auth one — two bugs stacked.
          DIGEST+="**Generated:** $(date -u '+%Y-%m-%d %H:%M UTC')\n\n"
          DIGEST+="## Auto-Rejected Issues\n\n"
          DIGEST+="These issues were auto-rejected but may have salvageable content.\n\n"
          DIGEST+="| Issue | Title | Created | Action |\n"
          DIGEST+="|-------|-------|---------|--------|\n"

          # Parse issues
          echo "$ISSUES" | python3 -c "
          import sys, json
          issues = json.load(sys.stdin)
          for issue in issues:
              num = issue['number']
              title = issue['title'][:50]
              created = issue['createdAt'][:10]
              print(f'| #{num} | {title} | {created} | [Review](https://github.com/Ikalus1988/MisakaNet/issues/{num}) |')
          " >> /tmp/digest_table.txt

          DIGEST+=$(cat /tmp/digest_table.txt)
          DIGEST+="\n\n## Salvage Actions\n\n"
          DIGEST+="For each rejected issue, you can:\n"
          DIGEST+="1. **Improve** — Add missing content and re-submit\n"
          DIGEST+="2. **Convert** — Manually create lesson if valuable\n"
          DIGEST+="3. **Close** — Reject permanently if not useful\n\n"
          DIGEST+="## How to Improve\n\n"
          DIGEST+="To improve a rejected issue:\n"
          DIGEST+='1. Add a `## Error` section with error messages\n'
          DIGEST+="2. Add code blocks with examples\n"
          DIGEST+='3. Add verification steps in `## Verification`\n'
          DIGEST+="4. Remove user-specific paths\n"
          DIGEST+="5. Add more technical detail\n\n"
          DIGEST+="---\n"
          # Single quotes: inside double quotes the backticks would be command
          # substitution, so the shell tried to execute `/salvage-review` (exit 127).
          DIGEST+='_This is an automated digest. Run `/salvage-review` to manually review issues._'

          # Save digest
          echo -e "$DIGEST" > /tmp/salvage_digest.md

          # Post as issue comment or create new issue
          # `--jq` instead of piping through python3: the previous one-liner called
          # json.load(sys.stdin) twice, and the second call reads an exhausted
          # stream (JSONDecodeError on any non-empty result).
          DIGEST_ISSUE=$(gh issue list --repo ${{ github.repository }} \
            --label "salvage-digest" \
            --state open \
            --json number \
            --limit 1 --jq '.[0].number // ""')

          if [ -n "$DIGEST_ISSUE" ]; then
            # Update existing issue
            gh issue comment $DIGEST_ISSUE --repo ${{ github.repository }} \
              --body-file /tmp/salvage_digest.md
          else
            # Create new issue
            gh issue create --repo ${{ github.repository }} \
              --title "📋 Intake Salvage Digest - $(date -u +%Y-%m-%d)" \
              --body-file /tmp/salvage_digest.md \
              --label "salvage-digest,needs-human-review"
          fi

      - name: Cleanup old rejected issues
        run: |
          # Auto-close rejected issues older than 30 days
          ISSUES=$(gh issue list --repo ${{ github.repository }} \
            --label "auto-rejected" \
            --state open \
            --json number,createdAt \
            --limit 100)

          echo "$ISSUES" | python3 -c "
          import sys, json
          from datetime import datetime, timedelta

          issues = json.load(sys.stdin)
          cutoff = datetime.now() - timedelta(days=30)

          for issue in issues:
              created = datetime.fromisoformat(issue['createdAt'].replace('Z', '+00:00'))
              if created < cutoff:
                  print(issue['number'])
          " | while read ISSUE_NUM; do
            gh issue close $ISSUE_NUM --repo ${{ github.repository }} \
              --comment "Auto-closed after 30 days. This issue was auto-rejected and not salvaged." \
              --reason "not-planned" || true
          done

      # The other half of the same promise. The steps above cover intakes that were rejected and might
      # still be worth saving; this covers the opposite state — the work IS on `main` and the issue is
      # still open, so the reporter cannot tell "nobody looked" from "we fixed it and said nothing".
      # Seven of those were found by hand on 2026-09-21 (#1472/#1473/#1635/#1574/#1618/#1619 …), which
      # is the definition of a rule that needs a mechanism: SOP §3 is an iron rule that a person has to
      # remember, and remembering is not a mechanism (#2040).
      #
      # It reports and stops. It never comments on the intakes it finds and never closes one: the
      # receipt in SOP §3 is a human sentence addressed to a reporter, and a bot that closes an issue
      # because a filename matched would be the worst possible version of this automation.
      - name: Report intakes whose work is already on main (#2040)
        run: |
          set -euo pipefail

          python3 scripts/done_but_open.py --json-out /tmp/done_but_open.json > /tmp/done_but_open.md
          count=$(python3 -c "import json; print(len(json.load(open('/tmp/done_but_open.json'))))")

          if [ "$count" = "0" ]; then
            # Silent on purpose: a report that fires every day with nothing in it is the alarm people
            # mute. The step summary still records that it ran, so "no findings" and "never ran" stay
            # distinguishable.
            {
              echo "### Done but open"
              echo ""
              echo "No intake is in the done-but-unannounced state."
            } >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi

          cat /tmp/done_but_open.md >> "$GITHUB_STEP_SUMMARY"

          DIGEST_ISSUE=$(gh issue list --repo ${{ github.repository }} \
            --label "salvage-digest" \
            --state open \
            --json number \
            --limit 1 --jq '.[0].number // ""')

          if [ -n "$DIGEST_ISSUE" ]; then
            gh issue comment "$DIGEST_ISSUE" --repo ${{ github.repository }} \
              --body-file /tmp/done_but_open.md
          else
            gh issue create --repo ${{ github.repository }} \
              --title "📋 Intake Backlog Digest - $(date -u +%Y-%m-%d)" \
              --body-file /tmp/done_but_open.md \
              --label "salvage-digest,needs-human-review"
          fi
