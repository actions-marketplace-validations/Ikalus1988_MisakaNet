name: fatal-guard CI

on:
  push:
    # `branches` (not just `paths`): a paths-only filter does NOT exclude tag pushes, so pushing a
    # tag — the intake bot's `v1`, or a hand-made release tag — ran this workflow against a commit
    # that main already tested. `**` matches every branch, so branch behaviour is unchanged.
    branches: ['**']
    paths:
      - 'packages/fatal-guard/**'
  pull_request:
    paths:
      - 'packages/fatal-guard/**'
  workflow_dispatch:

permissions:
  contents: read

# Same reason as ci-cross-platform.yml: a fan-out over operating systems that cannot
# cancel a superseded pull-request run lets the replaced one keep holding runners.
concurrency:
  group: fatal-guard-${{ github.head_ref || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    # No bound meant a hung leg held its runner for the default six hours.
    timeout-minutes: 30
    # paths filters already gate push/PR; keep workflow_dispatch always runnable.
    # Avoid a job-level if that can leave 0 jobs and mark the workflow as failed.
    defaults:
      run:
        working-directory: packages/fatal-guard

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7
        with:
          node-version: '22'

      - name: Check zero dependencies
        run: |
          node -e "const p=require('./package.json'); const n=Object.keys(p.dependencies||{}).length; console.log('dependencies:', n); if (n !== 0) process.exit(1)"

      - name: Test module loads without error
        run: |
          node -e "require('./register'); console.log('OK: module loaded')"

      - name: Test uncaughtException invokes handler
        run: |
          node --test tests/smoke.test.js

      - name: Test legacy verification suite
        run: |
          node tests/verify-fix.js

      - name: Test TypeScript declaration exists
        run: |
          node -e "const fs=require('fs'); if (!fs.existsSync('index.d.ts') || !fs.readFileSync('index.d.ts','utf8').includes('FatalPayload')) process.exit(1); console.log('PASS: index.d.ts')"

      - name: Run package tests
        run: npm test
