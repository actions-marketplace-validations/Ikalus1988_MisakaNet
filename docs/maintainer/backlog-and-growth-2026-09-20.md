# 待办优先级 · 增长 · 短板：一次合并评估（2026-09-20）

这份文档把三样东西放在一起，因为它们互相约束：**待办清单**（哪些该做）、**增长分析**（做哪件对漏斗有作用）、
**短板分析**（哪件是结构性欠账）。它同时核对了一份第三方评审（同日的《MisakaNet 最新进展评估》），
因为那份评审里有多处数字与实测不符——**误差最大的一项差 8 倍**，而它恰好被列为"当务之急"。

数据来源与时间：GitHub API / 本地 git / Cloudflare D1 / npm 与 PyPI registry / `/api/health`，
全部在 **2026-09-20** 当天实测。凡引用评审原文的，后面标"评审"。

## 1. 核心指标快照（实测）

| 指标 | 实测值 | 口径 / 备注 |
|---|---|---|
| 版本 | PyPI & manifest **2.31.0**；npm **2.30.2** | npm 落后一版；自动发版已接线，等一次 release/审批 |
| 课程 | **393**（受 CI 管）/ **405**（D1 `lessons`）/ **452**（`find *.md`） | 三个口径都在用，对外只讲 393 → 需要说清 |
| 课程目录 | `lessons/` 下 **13** 个子目录 | 另有 `draft/` 与 `drafts/` **两个只有 README 的重复目录** |
| 远端分支 | **267**（>60 天 22 / 31–60 天 26 / 8–30 天 89 / ≤7 天 130） | 评审说 594（416 未合并）→ 错 8 倍 |
| 近 5 天提交 | **295** | 评审说 ~50 → 错 6 倍 |
| 提交作者（近 5 天） | Ikalus1988 **143** / misakanet-bot **129** / zsxh1990 13 / 其他 5 | 评审的 "1172 vs 889+303" 无法复现 |
| 合并 PR 作者（最近 100 条） | Ikalus1988 **87** / zsxh1990 6 / goanscryca 1 / Saadat555 1 | **bus factor 1** 的直接证据 |
| open issues / PRs | **51 / 16** | 其中 11 条外部 PR 挂着，最老 4 天 |
| 注册节点 | **357** | 与站点徽章一致 |
| 包下载（30 天） | PyPI `misakanet-core` **8,774** ≫ PyPI `misakanet` 1,134 ≫ npm `misakanet` **501** | **最容易拿走的不是安装器，是引擎** |
| 需求信号 | `gap` **83** 个未命中家族 | 头部是 "monolithic 5000-line frontend…"（×4）|
| Benchmark（`docs/benchmarks/latest.json`，987 runs）| `with_lesson` 命中率 **0.466** vs `plain` **0.234**（≈2×）| 评审引用 0.489~0.667：0.667 是**单次 run** 的字段值。⚠️ **这个数是复述率，不是检索/正确率**：`lesson_hit_rate` 就是「注入的那篇课的修复命令有多少出现在回答里」，scenario 取自**同一篇课的标题**、全程没有检索调用（`METRIC_SUMMARY`，`scripts/benchmark_workers_ai.py`）；内部复核见 `docs/maintainer/capability-inventory-new-user-2026-09-18.md:21`（「同一脚本既出题又打分」）。README 的公开口径已于 2026-09-25 改成同一句，并由 `tests/test_benchmark_claims.py` 守住 |
| Benchmark 反向信号 | `actionable` 0.763（with）vs 0.830（plain）| **评审漏了**：命中率翻倍的同时可操作性下降 |
| 站点流量 | GitHub UV ~176（`growth-funnel.md` 旧基线）；KV 读 ~4,800/天 | 无法区分人 / agent / CI |

## 2. 对第三方评审的逐条核对

**错的（附实测）**

| 评审 | 实测 |
|---|---|
| 远端分支 594（416 未合并） | **267**；>60 天只有 22 个。近 7 天 130 个 → 大多是活跃 PR 分支，"批量清理"会伤开放 PR |
| 课程 402+ / 19 目录 | 393 / 405 / 452 三种口径；13 个子目录 |
| 近 5 天 ~50 次提交 | **295** |
| 贡献占比 1172 vs 889+303 | 无法复现；见上表 |
| handoff 在 `docs/handoff-2026-09-06.md`、`docs/handoff-final` | 路径**不存在**；真实是 `docs/maintainer/handoff-*.md`（最新 09-19）→ "maintainer 轮换迹象"依据不成立 |
| "未发布的 release v2.29.0" | **已发布**（Releases 列表里） |
| "#1527 已拆出"（暗示待做） | **closed / completed**，且已实现（`scripts/failure_harvest.py`、`misakanet/cli/heal.py`、`tests/test_failure_harvest.py`） |
| Benchmark with_lesson 0.489~0.667 | 聚合 **0.466**；0.667 是单次 run 的值 |

**对的（已采纳）**

- `docs/frontend-status.md` 停在 **v2.18.0 / 2026-08-23**，而仓库已到 2.31.0、首页与 README 都大改过 → 真缺陷。
- `#1525` intake-bot 已上线（closed/completed 2026-09-07）。
- AAIF 分支 `docs/aaif-blog-submission` 存在（落地成本最低的外部曝光路径）。
- 贡献者真实存在：laurentketterle-hub 66 条、2lll5 37 条、s6pa1rta3n-lab 10 条（但**合并**的极少）。
- KV 监控增强 / npm 自动发版 / release-please 版本漂移修复 —— 均属实（本仓当日完成）。

## 3. 待办优先级（合并评审后的结论）

排序依据：**是否直接作用于漏斗** × **是否结构性风险** ÷ 成本。凡"评审提出但我改了位置/改法"的，注明理由。

**P0 —— 本周，直接作用于漏斗**

1. **补发 npm 2.31.0**：自动发版已接线，human-in-the-loop 只剩"批准 run"。分发面自己漂是最便宜的损伤。
2. **审核队列 48h SLA**：11 条外部 PR 挂着（最老 4 天），而最近 100 条已关闭 PR 里外部合并仅 8 条。
   对贡献者而言，"48 小时没有回应"就是"这个仓库不活跃"。
3. **补 funnel 埋点**（走 D1，别走 KV）：`funnel.visit / register / first_search / drop_off` 目前在**全仓零埋点**
   （grep 为空），`docs/analytics/first-call-funnel.md` 的实现段还指向早已迁走的 `hub/master/master_api.py` 与 Grafana。
   **我们无法回答"装完有没有人真的调用"**——这是增长上最贵的洞，评审把它放在"外部增长"之后，位置不对。
4. **#1895 事实同源**：本会话已修 3 起同类漂移（README 版本、根 `llms.txt` 317 vs 393、`docs/mcp.md` 假资源）。
   首页命令与 README/llms.txt 必须交给一个写作者。

**P1**

5. KV 写入家族搬家（等一天排行数据，只搬第一名，KV 留回退）——#1890。
6. `docs/mcp.md` 声明了 5 个不存在的 `misaka://` 资源 + 一个测了等于没测的测试——#1901。
7. 站点三项：#1892（导航 + `/install` 孤儿页）、#1893（复制按钮）、#1894（i18n 一致性测试）。
8. **`docs/frontend-status.md` 更新到当前版本**（评审，采纳）。
9. **公开 E0–E4 分布**（评审，采纳为第一步）：先出数字，再谈"E0/E1 六个月无复用降级"的机制。

**P2**

10. Benchmark 口径修正 + 可视化入口：README 只写了 08-30 那组，最新是 **0.234 → 0.466**，且 `actionable` 下降
    应一并写进诚实段落。
11. 分支 housekeeping（**改法**：只清 >60 天，共 22 个；有开放 PR 的一律不碰）。
12. intake bot 的 suggest-only 优先（评审；需要接入方数据支撑，目前 intake 仅 19 条）。
13. #1896（`/start` 分工）、#1897（站点埋点）。
14. 合并 `lessons/draft/` 与 `lessons/drafts/` 两个空目录。

**P3**

15. AAIF blog 落地（分支已在，纯执行）。
16. Cloudflare 开发者 onboarding 内容。
17. 给 `release` 环境加 tag 策略，恢复 `@misaka-net/misakanet-setup` / `fatal-guard` 的 tag 发布。

## 4. 增长分析

**漏斗**：发现 → 分发 → 接入/激活 → 复用 → 贡献回馈。

**五个泄漏点（每条都有证据）**

1. **激活不可测**：funnel 四指标零埋点（见 P0#3）。
2. **最容易拿走的是引擎，不是能力**：`misakanet-core` 8,774/月 vs 安装器 501/月。产品承诺是"你的 agent 会主动查"，
   而多数下载走的是"把检索库拿走自己集成"——中间那条断层没有任何人测过。
3. **分发面自己在漂**：npm 落后一版；对外说 393 而 D1 有 405。
4. **审核队列是人因瓶颈**：11 条挂着；外部合并占比 8/100。
5. **需求信号没闭环**：83 个未命中家族（"monolithic frontend / 909-line app.js" 这类**通用软件工程重构**需求）
   没有对应的"下周该写什么"排期动作。注意它同时说明：**未命中主要是语料缺口，不是检索能力**。

## 5. 仓库短板（工程）

1. **bus factor 1**：95/100 合并来自一人；历史 CI 积压 1,804 条 held runs。
2. **免费档 KV 额度常年不够**：12 天里 9 天超（#1890），今天 06:43 UTC 又打满；根因是"按实体建键"的写入形状。
3. **可观测性欠账**：health 曾 40% 概率骗人；KV 错误分类器两次被生产纠正（`[code: 10048]` → 再到
   `KV put() limit exceeded for the day.` 这种无码措辞）；funnel 零埋点。
4. **"同一事实多个面"反复出现**（已修 3 起）；仓库**没有 link checker，也没有 markdown lint**。
5. **测试结构缺口**：3 个 MCP 模块在本地因 `mcp` 版本无法收集（CI 用 pin 版本）；历史上 `audit` 的
   pytest 步骤 `continue-on-error` 会掩盖失败。
6. **i18n / 文档一致性靠人工**：i18n 一致性测试尚不存在（#1894）；`setup_docs` 测试只覆盖 4 个入口。

## 6. 用户与产品维度

| 层 | 是谁 | 证据 | 现状 |
|---|---|---|---|
| **A 使用方 agent（主用户）** | Claude Code / Codex / Cursor… | MCP 匿名读、19 条 agent intake | 读不限次（2026-09-18 起）——最好的待遇给了主用户 ✅ |
| **B 集成方开发者** | 自己写代码要检索 | `misakanet-core` 8,774/月 | 有库有文档，但没有"接入后是否生效"的验证路径 |
| **C 贡献者** | zsxh1990、laurentketterle-hub、2lll5… | 提交/PR 真实存在 | **回应周期不可预期** ⚠️ |
| **D 人类访客 / 评审** | stars 493、UV ~176 | GitHub API | 能看，但没有复访理由 |

**产品缺口**

- **没有"我"**：无登录、节点是化名（刻意的隐私设计）→ 无法做留存/复访，也没有组织/团队面。
- **没有状态与承诺面**：`/api/health` 刚修好，但没有公开状态页；对"agent 依赖的基础设施"缺少 SLA 表述。
- **只有 pull，没有 push**：价值依赖 agent 在正确时刻主动调用；没有 IDE 插件、CI 集成或主动提示。
- **激活路径未闭环**：安装器写了配置，但没有回执证明它生效（`--report` 是手动的）。
- **定位张力**："zero bounty + maximum rigor"是好的贡献筛选器，但对留存是负向的：没有金钱、署名机制弱、审核慢。

## 7. 一句话分歧

评审把"分支堆积"排第一，位置不对——**用户与 agent 看不见分支**，而"npm 落后一版 / 11 条 PR 无回应 /
激活完全不可测"这三件才直接作用于漏斗。它的"命中率是杠杆"方向对，但因果要分清：**0.234 → 0.466 是注入课程
带来的提升，未命中集中在 gap 家族的通用重构需求上，所以是补语料，不是改检索**；而它建议的 #1527 那块已经交付。
